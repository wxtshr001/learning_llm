# 0001R 短测：Linear 输出索引与 bias

请在看完补强课后关闭页面，闭卷作答。不要运行代码。

## 1. 公式

```text
X: [2,3]
W: [3,4]
b: [4]
```

写出 `Y[1,2]` 的完整公式，展开所有三项乘法，不使用冒号写法。

## 2. 手算

```text
X = [[1, 2],
     [3, 4]]

W = [[2, 1],
     [0, 3]]

b = [5, 7]
```

计算完整的 `Y = X @ W + b`。

## 3. 为什么是 b[j]

用一句话解释：为什么 `Y[i,j]` 加的是 `b[j]`，不是 `b[i]`？

## 4. 迁移

```text
hidden_states: [seq=6, hidden=128]
Wq: [128, 96]
bq: [96]
```

写出 `Q[4,10]` 的索引公式，并说明它使用哪个 bias 元素。

## 通过标准

四题全部正确。该短测只检查已经暴露的断层，不重新评价已通过的代码实现。
