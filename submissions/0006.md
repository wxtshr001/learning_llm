# 0006 提交

## 探索脚本完整输出

```text
tmp/output6e.txt
```

## 独立作业完整输出

```text
tmp/output6s.txt
```

## 1

1.[1,0,0,0],[0.5,0.5,0,0],[1/3,1/3,1/3,0],[0.25,0.25,0.25,0.25]
2.[2,3,14/3,15/2]
3.因为qk全都是1，没有相似度差异，softmax后权重均匀。因为需要强制屏蔽未来token防止将结果参与到计算里。

## 2

Q: [2,5,4]
K: [2,5,4]
V: [2,5,4]
[2,4,5]
scores: [2,5,5]
mask: [2,5,5]
weights: [2,5,5]
output: [2,5,4]
q是行，k是列，softmax沿k轴将k轴方向位置的数字做softmax

## 3

1.会影响q4和以后的内容
2.会影响q2和以后的内容
3.softmax后再加mask会导致k方向上权重总和不为1

## 4

1.对于gpt这种生成式decoder-only的llm来说，q是代表当前输入token想要去查的匹配向量，k代表当前候选token对外的特征，v是代表被查token的真正内容向量信息
2.scoreij=Qi*Kj^T
outputi=sumj(weightij*Vj)

## 5

为了让Q*K后膨胀的数值缩放到正常分布范围，防止softmax饱和。不变。Q*K是D维度相乘导致的膨胀，用sqrt(s)无法稳定softmax输出

## 6

1.代码无异常只是代表语法和shape匹配，不代表数值正确
a.核对shape是否正确
b.修改靠后的v，看前面的q不变后面的q发生变化
c.weight每行和为接近1
d.构造小规模输入手算输出
2.hidden state->rms->qkv proj->residual->rms->ffn->residual，还没有将状态映射到词表维度接softmax，因此不是next-token logits

## 我仍然卡住的地方

-
