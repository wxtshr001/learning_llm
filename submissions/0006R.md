# 0006R 提交

## 引导脚本输出

```text
tmp/output6R1.txt
```

## 独立代码输出

```text
tmp/output6R2.txt
```

## 1. 两个逐元素公式

scores[b,q,k]=(sum 0->d-1 Q[b,q,d]*K[b,k,d])/sqrt(D)
沿D方向求和，保留b q k

output[b,q,d] = sum 0->S-1 (softmax(scores[b,q,:]k)*V[b,k,d]
沿k方向求和，保留b,q,d

## 2. Decoder block 路径

tmp/test

经过rms norm和lm head

## 3. 代码 contract

tmp/output6R2.txt

因为D维度也不能是0
需要和query保持device同步才能进行计算

## 我仍然卡住的地方

-
