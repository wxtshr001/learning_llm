# 第 0001 课反馈

## 结论

**76/100，针对性补强，暂不进入第 0002 课。**

代码测试通过，但关键题 2 未通过；同一 bias 索引误区也出现在题 6，题 3 的完整矩阵手算错误。按照课程门禁，完成 0001R 四题短测后再判定。

## 分项

| 证据 | 得分 | 判断 |
|---|---:|---|
| 概念解释 | 14/20 | 理解 Linear 改变 hidden/out 维，不混合 token；广播解释偏简略 |
| shape/计算 | 16/30 | 输出 shape 正确；`b[i]`/`b[j]` 未稳定，矩阵手算错误 |
| 实现与测试 | 32/35 | 8 项测试通过，辅助函数思路正确；代码注释仍错误写成 `b[i]` |
| 迁移题 | 14/15 | Q shape、Wq 含义和不跨 token 混合均基本正确 |

## 逐题

1. 正确：输出为 `[3,2]`。
2. 未通过：应使用 `b[0]`，并明确展开对 k 的求和。
3. 未通过：正确结果为 `[[11,24],[13,28]]`。
4. 基本正确：更精确的说法是广播从末尾维度对齐，`[2]` 与 `[3,2]` 的最后一维匹配。
5. 正确：seq/token 维保留，hidden 4 被投影到 out 2。
6. 部分正确：W 应为 `W[k,j]`，bias 仍应由 `b[i]` 改为 `b[j]`。
7. 正确：`Q=[5,64]`；Wq 是 Q 投影权重；Linear 独立处理 token，不在 token 间混合。

## 下一步

完成 `lessons/0001R-linear-output-index.html` 和 `assessments/0001R-linear-output-index.md`。四题全对后，第 0001 课通过。
