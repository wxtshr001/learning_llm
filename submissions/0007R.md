# 0007R 提交

## 引导脚本完整输出

```text
hidden rank/shape: 3 (2, 3, 8)
q_raw rank/shape: 3 (2, 3, 8)
k/v_raw rank/shape: 3 (2, 3, 4)
Q split [B,Nq,S,D]: (2, 4, 3, 2)
K/V stored [B,Nkv,S,D]: (2, 2, 3, 2) (2, 2, 3, 2)
K/V logical [B,Nq,S,D]: (2, 4, 3, 2) (2, 4, 3, 2)
scores/weights [B,Nq,S,S]: (2, 4, 3, 3)
head_output [B,Nq,S,D]: (2, 4, 3, 2)
merge [B,S,Nq*D]: (2, 3, 8) rank: 3
explicit Nq head axis: Q, logical K/V, scores/weights, head_output
no explicit head axis after merge or o_proj
index check k_raw[1,2,3] == K[1,1,2,1]: 43.0 43.0
Lesson 0007R guided stage trace passed.
```

## 独立脚本完整输出

```text
Lesson 0007R: all GQA stage-shape checks passed.
      hidden: (3, 5, 32)
       q_raw: (3, 5, 32)
       k_raw: (3, 5, 8)
       v_raw: (3, 5, 8)
     q_split: (3, 8, 5, 4)
     k_split: (3, 2, 5, 4)
     v_split: (3, 2, 5, 4)
   logical_k: (3, 8, 5, 4)
   logical_v: (3, 8, 5, 4)
      scores: (3, 8, 5, 5)
     weights: (3, 8, 5, 5)
 head_output: (3, 8, 5, 4)
       merge: (3, 5, 32)
    o_output: (3, 5, 32)
```

## 1. Tensor 阶段与显式 head axis

1.[1,6,24] [1,6,24] [1,6,12] [1,6,12]
2.
| 张量名称 | 操作细节 | Shape |
| :--- | :--- | :--- |
| `q_split` | `.view(1,6,6,4).transpose(1,2)` | **(1, 6, 6, 4)** |
| `k_split` | `.view(1,6,3,4).transpose(1,2)` | **(1, 3, 6, 4)** |
| `v_split` | `.view(1,6,3,4).transpose(1,2)` | **(1, 3, 6, 4)** |
| `logical_k`（映射后） | `k_split.repeat_interleave(2, dim=1)`（因 \( Nq/Nkv = 2 \)） | **(1, 6, 6, 4)** |
| `logical_v`（映射后） | `v_split.repeat_interleave(2, dim=1)` | **(1, 6, 6, 4)** |
| `scores` | \( Q \cdot K_{logical}^\top / \sqrt{4} \)，未加掩码前 | **(1, 6, 6, 6)** |
| `weights` | `scores` 应用 Causal Mask + Softmax 后 | **(1, 6, 6, 6)** |
| `head_output` | `weights @ V_logical` | **(1, 6, 6, 4)** |
| `merge` | `.transpose(1,2).contiguous().view(1,6,24)` | **(1, 6, 24)** |
| `o_proj output` | `merge @ W_o`，输出维度保持 24 | **(1, 6, 24)** |

3.q_split logical_k logical_v scores/weights head_output
没有，merge 将 Nq 和 D 合并成了最后一维 Nq*D（即 24），它不再是独立分开的“头数”轴，而是变成了“特征长度”轴。

o_proj 是一个线性层（权重矩阵 [24, 24]），它对特征进行线性混合，输出形状 (B, S, 24)，彻底消除了头的概念。此时 Nq 的信息被压缩进了特征空间的混合权重中。

4.k_split v_split
因为 GQA 的 Key/Value 投影矩阵 \( W_k, W_v \) 的输出维度是 **`Nkv * D`**，即 \( 3 \times 4 = 12 \)。它代表压缩后的总特征数，只生成 3 个头对应的特征，而非 6 个。
没有。`raw_k` / `raw_v` 的形状是 `(1, 6, 12)`，此时它只是一个普通的 2D 矩阵（序列长度 6 × 特征维度 12）。**独立的 Head Axis（即 `Nkv=3`）只有在执行 `.view(1, 6, 3, 4).transpose(1,2)` 之后才会显式出现。

5.[2,4,20,40]
16.5
直接平均会消除特征，导致表达能力丢失，后面的 Linear 无法恢复丢失的信息

## 我仍然卡住的地方
