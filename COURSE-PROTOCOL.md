# 自适应课程协议

## 每节课的固定结构

1. **进入条件**：列出必须已经掌握的能力。
2. **本节成果**：一个能运行、能解释或能测量的具体结果。
3. **指定资料**：只读当前任务需要的官方章节。
4. **引导练习**：在提示下完成最小实验。
5. **独立作业**：不提供完整实现。
6. **闭卷测试**：检查能否迁移到新数字和新 shape。
7. **提交与反馈**：检查答案、代码、解释和调试过程。
8. **门禁决策**：通过、针对性补强或重做。

## 判定规则

每节课按四类证据判断：

| 证据 | 权重 | 说明 |
|---|---:|---|
| 概念解释 | 20% | 能否用自己的话解释“为什么” |
| shape/计算 | 30% | 能否在新输入上正确推导 |
| 实现与测试 | 35% | 代码是否通过，以及是否理解测试失败 |
| 迁移题 | 15% | 能否把本节能力映射到 LLM 组件 |

门禁：

- 80 分以上且所有关键项通过：进入下一课。
- 65～79 分，或只有一个局部关键项失败：生成 20～40 分钟补强课。
- 65 分以下，或两个以上关键项失败：降低抽象程度重学本课。

代码运行成功不是自动通过；如果无法解释 shape 或只是复制实现，仍需补测。反过来，若概念理解正确但只是 Python 语法错误，则不会要求重学数学，而是补 Python 适应练习。

## 反馈后会更新什么

- 在 `learning-records/` 中记录已经证明掌握的能力或纠正过的误区。
- 在 `NOTES.md` 中记录有效的教学方式与持续困难。
- 只生成下一节最合适的课程，不提前锁死后续难度。
- 如果主项目或设备实际情况变化，再调整 `CURRICULUM.md` 与 `LEARNING-ROUTE.md`。

## 提交方式

优先直接在聊天中提交：

```text
第 0001 课
代码运行结果：...
1. ...
2. ...
遇到的问题：...
```

也可填写 `submissions/NNNN.md`。Codex 检查后会给出逐项证据、分数、门禁结论和下一步。
